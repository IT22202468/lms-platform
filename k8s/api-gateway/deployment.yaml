apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: lms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: ghcr.io/your-github-owner/api-gateway:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: lms-secrets
                  key: JWT_SECRET
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=70.0 -XX:InitialRAMPercentage=30.0 -Xss256k -XX:+UseSerialGC -XX:MaxMetaspaceSize=64m"
            - name: CORS_ALLOWED_ORIGINS
              value: "https://otterspacelearn.vercel.app"
            - name: AUTH_SERVICE_URL
              value: "http://auth-service:8081"
            - name: COURSE_SERVICE_URL
              value: "http://course-service:8082"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 45
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            requests:
              memory: "80Mi"
              cpu: "50m"
            limits:
              memory: "160Mi"
              cpu: "250m"
